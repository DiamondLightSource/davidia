name: davidia
channels:
  - conda-forge
dependencies:
  - python == 3.10
  - fastapi
  - websockets
  - uvicorn
  - requests
  - nodejs
  - numpy
  - msgpack-python
  - pillow
  - pytest
  - pytest-asyncio
  - httpx
  - pip
  - ruamel.yaml
  - pip:
     - before-after # brings in mock==1.0.1 (can't use channel's as it's python < 3.8)
     - pydantic-numpy
     - orjson-pydantic

